# 《官渡密报》v0.9.1 试玩问题与存档系统修订方案

> 文档性质：v0.9 实机试玩后的强制修订规范  
> 适用版本：v0.9.1 及后续版本  
> 优先级：与 v0.9 基线并行；出现冲突时，本文件针对“存档、证据交互、人物图片、控件清晰度”的规则优先。  
> 核心原则：**启动先选档；查看不改布局；人物不拉伸；控件必须一眼可辨。**

---

# 1. 本轮为什么必须修订

v0.9 已经解决了“开局直接审讯、巨大标题、组件失控”等结构问题，但实际试玩继续暴露出四类严重问题：

1. 浏览器/服务器重新启动后可能直接恢复到中途，玩家不知道自己加载了哪一份进度；
2. 没有正式存档选择页面，无法直观看到自己玩到哪一幕、当前任务是什么；
3. 证据比较页的“选择、查看、放大”行为混在一起，出现无法继续、卡片一大一小、无法还原等现象；
4. 人物图片在不同容器中尺寸策略不统一，出现裁切、拉长、背景烘焙与人物叠加的问题；
5. 深色背景、深色卡片、低对比按钮同时存在，导致控件和场景混在一起。

因此 v0.9.1 不增加新的案件功能，优先修正“能否稳定玩、是否知道当前进度、界面是否清楚”的基础体验。

---

# 2. 启动流程重构

## 2.1 启动服务器 / 刷新浏览器

无论本地是否存在存档：

```text
启动游戏
↓
标题页
↓
进入存档
↓
存档选择页
↓
玩家主动选择某一档
↓
恢复该档游戏位置
```

禁止：

```text
检测到 localStorage
↓
自动跳回审讯 / 推理 / 投饵页面
```

原因：自动恢复会让玩家失去“我现在载入的是哪一档、我玩到哪里”的控制感。

---

# 3. 存档系统正式规格

## 3.1 第一版提供三个本地存档槽

固定显示：

- 存档 1
- 存档 2
- 存档 3

不做无限存档列表，避免原型阶段增加无意义管理成本。

## 3.2 每个已有存档必须显示

- 存档名称；
- 当前幕；
- 当前场景；
- 当前任务；
- 调查进度百分比；
- 已记录线索数量；
- 已建立推断数量；
- 最后保存时间。

示例：

```text
存档 1 · 官渡案卷
第三幕 · 碎片成军情
当前位置：还原双渠道泄密链
当前任务：判断时辰、路线和传递方式如何拼合
调查记录：11 条线索 · 4 条推断
最后保存：08-28 12:18
```

玩家不需要进入游戏，就能知道自己玩到哪里。

## 3.3 空存档

显示：

```text
空案卷
从序章开始
时代背景、身份介绍和基础引导都会从头播放。
```

操作：

> 【新建存档】

新建后进入 v0.9 序章。

## 3.4 已有存档操作

主要操作：

> 【继续此档】

辅助操作：

> 【删除】

删除必须二次确认。

## 3.5 自动保存

玩家选择某个存档后，该次游戏会绑定到当前槽位。

以下行为发生后自动保存：

- 场景切换；
- 获得关键线索；
- 建立推断；
- 完成审讯；
- 完成曹操复命；
- 完成投饵；
- 提交最终报告。

原型阶段不增加“手动按 F5 保存”等复杂机制。

## 3.6 旧 v0.9 / v0.8 存档兼容

首次进入 v0.9.1 时：

- 如果没有新存档槽；
- 但检测到旧 `guandu.current`；

则把旧自动存档导入“存档 1”，名称：

> 旧版自动存档

只迁移一次。

玩家以后删除该槽，不会因为旧 localStorage 再次自动出现。

---

# 4. 标题页修改

标题页不再直接提供“继续最近存档”。

只保留：

- 进入存档 / 开始新案；
- 设置。

若检测到存档：

> 【进入存档】

若没有存档：

> 【开始新案】

两者点击后都先进入存档选择页面。

这样启动行为始终一致。

---

# 5. 证据比较页重构

## 5.1 当前错误交互必须废弃

禁止：

- 点击卡片同时执行“选中 + 放大”；
- 选中后永久改变卡片尺寸；
- 左右两张证据因为点击产生一大一小；
- 查看详情后没有关闭/还原入口；
- 灰色按钮无法解释为什么不能继续。

## 5.2 新交互模型

每张证据卡只提供两个独立动作：

> 【选择这条】  
> 【查看详情】

选中以后：

> 【取消选择】

查看详情：

- 打开 Dialog；
- 页面背景和左右证据保持原尺寸；
- ESC、点击关闭或“看完了”均可退出；
- 关闭后不改变选中状态。

## 5.3 比较操作

页面明确显示：

```text
已选择 1 / 2
还需选择 1 条信息
```

选满后：

```text
两条信息已选齐
现在可以比较它们之间的关系。
```

主按钮：

> 【比较这两条信息】

未满足条件时允许 disabled，但必须同步显示锁定原因。

## 5.4 重置

新增：

> 【重置选择】

不需要返回上一页就可以重新选择。

---

# 6. 人物图片与场景分层规范

## 6.1 人物和背景必须分层

正确层级：

```text
场景背景
↓
人物前景
↓
对白 / 控件
```

不能继续依赖“一张人物 + 场景全部烘焙在同一张 CG”作为普通对话主结构。

CG 仍可用于重大镜头，但普通对白应优先独立人物层。

## 6.2 人物图片禁止拉伸

所有正式人物图：

```css
object-fit: contain;
object-position: center bottom;
```

禁止：

```css
width: 100%;
height: 100%;
object-fit: fill;
```

任何容器都不得通过改变宽高比例拉伸脸或身体。

## 6.3 人物资源方向

最终资源建议逐步制作透明背景 PNG/WebP：

- 曹操；
- 赵简；
- 陆淳；
- 郑禾；
- 杜衡。

现阶段若仍使用带背景人物图：

- 使用 contain；
- 边缘做柔和蒙版；
- 后方使用独立正常场景背景；
- 禁止人物矩形图片铺满整个舞台。

## 6.4 赵简审讯

原先带赵简人物的审讯 CG 不再作为整页背景。

改为：

- 独立暗色军帐背景；
- 赵简人物层；
- 对白和策略层。

这样人物尺寸可以独立控制，也不会重复出现两个赵简。

---

# 7. 控件清晰度规范

## 7.1 三层对比

### 背景层
最暗，负责环境。

### 内容层
比背景明显亮一个层级，负责阅读。

### 交互层
按钮、输入、可点证据必须最清楚。

## 7.2 主按钮

必须具备：

- 明确底色；
- 可见边框；
- hover；
- pressed；
- focus；
- disabled。

不能只靠非常微弱的金色文字变化表示“这是按钮”。

## 7.3 Disabled

Disabled 不能暗到像消失。

推荐：

- 仍可看清按钮形状；
- 饱和度降低；
- 同区域显示原因。

## 7.4 证据选中态

必须同时变化：

- 边框；
- 轻微位移；
- “已选择” Badge；
- 对应按钮变为“取消选择”。

只改变一个细边框不够。

---

# 8. 图片查看统一规则

任何人物、文书、地图、证据图片的“查看详情”都遵循：

```text
原页面布局保持不变
↓
Dialog / Sheet 单独显示详情
↓
关闭
↓
回到完全相同的原布局
```

禁止通过修改 Grid column、width、height 来实现放大。

---

# 9. 其他页面同步审核

v0.9.1 不只修截图页面，还必须检查：

- 人物首次登场；
- 赵简再问；
- 四人物调查；
- 案卷人物页；
- 曹操觐见；
- 投饵；
- 敌军回声；
- 最终报告。

检查问题：

1. 人物有没有被拉伸；
2. 控件是否和背景混在一起；
3. 是否存在点击后改变整个布局的卡片；
4. disabled 是否说明原因；
5. 是否存在无法还原的放大；
6. 1366×768 是否仍可完整操作；
7. 是否有按钮被其他层遮挡。

---

# 10. v0.9.1 强制验收标准

以下任何一条失败，不算完成：

- 启动服务器后直接跳进旧游戏场景；
- 没有存档选择页；
- 存档页看不出当前章节 / 场景 / 任务；
- 点击证据后左右卡片尺寸不一致；
- 查看详情不能关闭；
- 证据选齐仍无法继续；
- 人物图发生非等比拉伸；
- 控件和背景无法区分；
- disabled 按钮没有原因说明；
- 旧存档迁移后自动跳过标题/存档选择流程。

---

# 11. 后续项目基线

v0.9.1 以后，启动与存档统一遵守：

> **启动不等于读档；玩家选择存档才等于读档。**

图片交互统一遵守：

> **选择改变状态，查看只打开详情；两种行为永远分离。**

人物视觉统一遵守：

> **人物不承担背景职责，背景不决定人物比例。**

UI 统一遵守：

> **克制不等于模糊。游戏可以暗，但可操作元素必须清楚。**
