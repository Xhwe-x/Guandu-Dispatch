# 《官渡密报》v0.9 叙事流程与 UI 组件系统重构方案

> 文档性质：**v0.9.0 已实施基线 / 后续唯一 UI、叙事、交互标准**  
> 适用版本：Guandu Dispatch v0.9.0 及后续版本  
> 核心目标：从“AI 生成式古风页面”升级为“有节奏、有角色、有镜头、有组件纪律的三国军机悬疑游戏”  
> 本文优先级：高于此前零散 UI 美化说明；旧方案中与本文冲突的界面与流程要求，以本文为准。
> v0.9.1 补充：存档、证据选择/查看分离、人物图片比例与控件对比度，统一以《官渡密报_v0.9.1_试玩问题与存档系统修订方案.md》为准。

---

# 1. 本轮为什么必须重构

当前版本已经拥有案件、人物、证据、审讯、曹操复命、投饵、结局等核心内容，但用户实际试玩时仍然会第一眼判断出“这是 AI 做出来的界面”。

问题并不主要来自 AI 生图，而来自以下几个组合问题：

1. 字体尺度失控；
2. 页面标题过度巨大；
3. 大量绝对定位、固定尺寸导致内容超出视口；
4. 黑金、巨大文字、斜切背景、厚边框同时存在，视觉语言过重；
5. 一屏中多个区域争夺注意力，没有明确视觉中心；
6. 控件不像成熟组件系统，而像每个页面单独拼出来的视觉稿；
7. 剧情直接跳进“质询赵简”等中段流程，新玩家缺少背景、身份、人物与玩法认知；
8. 人物、文书、任务、对白虽然已经存在，却没有按照“逐步认识”节奏展示；
9. 过多内容一次性暴露，缺少游戏镜头和节奏；
10. CG、人物卡、按钮、任务面板在视觉上互相竞争。

当前截图中的“质询赵简”页面就是典型案例：

- 场景标题大到侵占角色与 CG；
- 标题在 16:9 横屏中仍无法稳定显示完整；
- 顶部导航与场景内容之间缺少层级过渡；
- 人物背景被巨大标题压住；
- 中间口供卡片尺寸偏大但信息密度偏低；
- 用户进入页面后会先注意“界面设计”，而不是“赵简说了什么”；
- 整体更像 AI 生成的概念板，而不是可以持续玩 30 分钟的商业游戏 UI。

因此 v0.9 不做“再把字体缩小一点”这种局部补丁，而是重新建立：

> **叙事节奏系统 + 组件系统 + 字体系统 + 场景布局系统 + 动效系统。**

---

# 2. 对四套现代 UI 系统的学习结论

本项目不会直接照搬现代 SaaS 视觉，但会借鉴它们的“组件纪律”。

## 2.1 从 shadcn/ui 学什么

核心不是白底圆角，而是：

- 一个组件只负责一件事；
- Button 有统一 variant 与 size；
- Card 明确拆成 Header / Content / Footer；
- Dialog 明确区分 Header / Description / Content / Footer；
- 组件支持尺寸和状态，而不是每页写一套 CSS；
- 长内容采用可滚动内容 + 固定操作区；
- 按钮、卡片、对话框均可组合、扩展，而不是“画死”。

《官渡密报》对应改法：

> 不复制 shadcn 的外观，复制它的**组件结构、尺寸系统、状态系统和可组合性**。

---

## 2.2 从 Magic UI 学什么

Magic UI 的价值不在炫技，而在“用很小的动效建立进入感”。

适合本项目借鉴：

- Blur Fade：人物、对白、场景标题柔和进入；
- Animated List：新线索、任务更新按顺序出现；
- Progressive Blur：CG 下缘与对白区的自然融合；
- Text Animate：仅用于章节标题、重大军令，不用于普通正文；
- Particles / Noise Texture：只用于夜袭、火场、军帐环境，不用于控件。

禁止：

- 每个按钮发光；
- 大量彩色渐变；
- 循环闪烁；
- 所有文字都做动画；
- 为“高级”而使用无意义运动。

原则：

> 动效只负责“告诉玩家发生了什么”。

---

## 2.3 从 Aceternity UI 学什么

Aceternity UI 的重点是 Micro Interaction（微交互）与视觉焦点迁移。

适合本项目：

- Hover Effect：人物、证据、任务列表之间切换焦点；
- Tooltip：解释锁定条件、状态、历史名词；
- 卡片轻微景深：只在人物档案、证据物件上使用；
- Floating Dock 思路：用于稳定的全局案卷 / 任务 / 提示入口，但需要改造成古代军机风格；
- Glowing / 3D Card 等仅作为灵感，正式游戏应明显弱化。

原则：

> “鼠标经过时知道能点”，而不是“鼠标经过就像科技网站”。

---

## 2.4 从 Naive UI 学什么

Naive UI 对本项目最有价值的是“统一主题变量 + 组件级 theme override”的设计思想。

本项目虽然使用 React，不直接引入 Naive UI，但应建立类似机制：

- 所有颜色来自 token；
- 所有字体大小来自 token；
- 所有圆角、间距、阴影、动画速度统一定义；
- 每个组件允许少量主题 override；
- 禁止页面临时写新的 #HEX 颜色和任意 font-size；
- 以后要换“更写实 / 更冷 / 更暖”主题，只改 token，不重写组件。

---

# 3. v0.9 总体验目标

新版打开后，用户不应直接看到大量按钮，也不应第一屏就进入审讯。

正确体验应该是：

```text
进入游戏
↓
了解时代与案件背景
↓
理解自己的身份与任务
↓
通过极简引导学会基本操作
↓
第一次看见关键人物
↓
第一次与人物对话
↓
从一句口供发现第一处可疑
↓
学习查看文书
↓
建立第一条简单推断
↓
逐步认识第二、第三人物
↓
案件规模扩大
↓
进入真正的审讯与自由调查
↓
向曹操复命
↓
继续追查完整泄密链
```

新版最重要的节奏词：

> **先理解，再操作；先认识人，再审人；先发现矛盾，再教推理。**

---

# 4. 新游戏完整开场流程

## 4.1 Scene 00：标题页

只显示：

- 《官渡密报》
- 一句副标题
- 继续案件（有存档时）
- 开始新案
- 案卷（有存档后）
- 设置

背景：曹军夜营 / 官渡营地动态场景。

不得显示：

- 一堆系统功能；
- 教程按钮堆叠；
- 开发版本状态；
- 大量解释性文字。

标题最大字号只允许在这里使用。

---

## 4.2 Scene 01：时代背景

目标：在 30–45 秒内解释玩家为什么在这里。

### 镜头 1

CG：黄河以南，曹袁对峙，远处军营火光。

字幕：

> 建安五年。曹袁相持于官渡，胜负悬于粮道。

无需按钮，2–3 秒后出现“继续”。

### 镜头 2

CG：押粮车队夜行。

字幕：

> 数日内，曹军粮道三遭袭扰。敌骑总能提前一步伏于要路。

### 镜头 3

CG：燃烧粮车。

字幕：

> 第三支粮队被截后，中军开始怀疑：军机正在从营中流出。

### 镜头 4

CG：中军大帐远景。

字幕：

> 你被临时调入军机案前，负责查清——敌军究竟从哪里得到消息。

过渡到“身份介绍”。

---

# 5. Scene 02：玩家身份介绍

不要弹一个长教程框。

画面以案桌 / 官印 / 文书展开。

显示三条简洁身份信息：

- 身份：查案佐吏 / 军机案查验人；
- 擅长：文书、驿传、粮运与地方路线；
- 权限：调阅相关军书、问询涉案人员、向主公阶段复命。

底部只出现一个按钮：

> 【接下此案】

点击后播放纸张与木案声音，进入基础引导。

---

# 6. Scene 03：新手引导——只教三件事

教程不允许一次告诉玩家所有系统。

第一次只教：

1. 点击物件；
2. 查看当前任务；
3. 随时返回案卷。

### 引导方式

案桌上只有一份“伏击军报”。

任务签：

> 当前任务：展开伏击军报。

玩家点击军报。

提示非常短：

> 可疑内容会在后续形成证据，不必现在记住全部内容。

关闭军报后：

> 新功能：案卷已开启。你可以随时回看人物、文书和任务。

不要立刻教笔迹、审讯、推演、投饵。

---

# 7. Scene 04：第一次人物出现——先认识赵简

玩家第一次见到赵简时，不能直接显示“质询赵简”。

正确节奏：

## 7.1 人物首次亮相

半身立绘缓慢进入。

标题：

> 赵简

副标题：

> 军书佐 · 负责誊写集合文书

一行简介：

> 他接触过出发时辰，却声称并不知晓粮队何时集合。

停留 1–2 秒。

按钮：

> 【听取口供】

---

## 7.2 第一段对白

不要一次摆出巨大人物图 + 大口供卡 + 大标题。

使用一条窄对话区：

**赵简**

> “属下只是照令誊写，并不知道粮队真正出发的时辰。”

玩家点击继续。

**查案官**

> “你负责誊写集合文书？”

**赵简**

> “是。但文书所写，并不等于最终军令。”

此时游戏不评价他说谎。

任务自动更新：

> 核对赵简是否真的无法得知集合时辰。

于是自然进入文书调查。

---

# 8. Scene 05：第一次“发现矛盾”教学

玩家获得两份东西：

- 赵简口供；
- 集合文书 / 出入记录。

这里第一次教学“证据比较”。

界面中只出现两个对象。

提示：

> 证据之间如果出现冲突，可以建立一条推断。

玩家点击：

> 赵简口供

再点击：

> 集合记录

系统高亮“时辰”。

第一次关系不用完全自由拖拽，采用引导式选择：

> 这两条信息的关系是？

- 相互印证
- 存在矛盾

玩家选“存在矛盾”。

反馈：

> 推断成立：赵简的口供需要进一步核实。

这时候才解锁“再次询问赵简”。

---

# 9. Scene 06：第二次见赵简——真正开始质询

这个页面才可以叫：

> **再问赵简**

而不是巨大“质询赵简”。

标题应该是场景标签，不是海报标题。

### 页面结构

顶部 52px 场景栏：

- 返回
- 第一折 · 再问赵简
- 案卷
- 任务
- 提示

主舞台：

- 左 / 中：赵简立绘与场景；
- 下部：对白；
- 右下或浮层：可使用证据。

玩家选择：

- 平询；
- 缓劝；
- 呈证质问；
- 威压；
- 设问。

但只有“呈证质问”时，证据槽展开。

这样降低一屏信息量。

---

# 10. 后续人物出现原则

陆淳、郑禾、杜衡都必须先“被介绍”，再成为功能入口。

## 陆淳

首次亮相：

> 邮驿主吏 · 掌驿舍文书流转与封检

先让玩家听他解释邮书制度，再发现粮簿问题。

---

## 郑禾

首次亮相：

> 军粮书佐 · 管车马簿与粮秣出入

先表现他圆滑、愿意配合，再让玩家发现他改了车马记录。

---

## 杜衡

首次亮相：

> 营外行商 · 长期供应草料、车具与灯油

先让玩家觉得“他只是商人”，再逐渐发现他能从采购量、车轮和道路条件推断军情。

人物必须承担“认知反转”，不能一出现就贴“嫌疑人”标签。

---

# 11. 新叙事阶段划分

## 第一幕：粮道疑云

目标：让玩家理解案件。

包含：

- 背景；
- 教程；
- 赵简；
- 第一份军报；
- 第一条矛盾。

---

## 第二幕：人人有隐情

目标：认识四个人物。

玩家发现：

- 陆淳改过簿；
- 郑禾撒谎；
- 赵简口供矛盾；
- 杜衡知道太多。

认知：

> 有秘密 ≠ 通敌。

---

## 第三幕：碎片成军情

目标：还原信息流。

玩家发现：

- 没有人掌握完整路线；
- 赵简只知道时间；
- 杜衡可以从物资与道路推断路线；
- 信息最终在杜衡手里完成拼合。

---

## 第四幕：觐见曹操

曹操不直接告诉答案，而是质询玩家证据。

这里第一次评价玩家：

- 谨慎；
- 切要；
- 冒进；
- 善布局。

---

## 第五幕：将计就计

玩家针对不同渠道投放不同假信息。

首次真正进入主动反情报。

---

## 第六幕：敌军回声

敌军行动成为新证据。

---

## 第七幕：最终军机报告

事实 / 责任 / 处置分开提交。

---

# 12. UI 总体布局重构

## 12.1 彻底取消“海报式场景标题”

普通场景标题不能再占画面高度 15%–25%。

### 正式规格

章节大标题：仅章节切换使用。

```text
32–40px desktop
28–34px laptop
```

普通场景标题：

```text
22–28px
```

人物名：

```text
20–24px
```

正文 / 对白：

```text
16–18px
```

辅助文本：

```text
13–14px
```

顶部导航文字：

```text
13–15px
```

禁止普通场景出现类似当前“质询赵简”约 70–100px 的巨大标题。

---

## 12.2 使用 clamp，而不是硬编码大字号

推荐：

```css
--font-scene-title: clamp(22px, 2vw, 30px);
--font-character-name: clamp(19px, 1.6vw, 24px);
--font-dialogue: clamp(16px, 1.05vw, 18px);
--font-body: clamp(14px, 0.95vw, 16px);
```

---

# 13. 视觉层级规则

每个页面只能有一个主视觉中心。

例如“再问赵简”：

1. 赵简；
2. 当前对白；
3. 当前可执行动作；
4. 案件辅助信息。

而不是：

- 大标题；
- 人物；
- 口供卡；
- 人物状态；
- 顶部导航；
- 大按钮；

全部一样显眼。

---

# 14. 页面宽度与安全区

桌面设计基线：1920×1080。

最低适配：1366×768。

### 内容安全区

```text
max-width: 1440px
width: min(94vw, 1440px)
```

### 页面边距

```text
desktop: 24–32px
1366: 16–24px
```

### 顶栏

```text
height: 48–56px
```

不得再做接近 75–90px 的厚顶栏。

---

# 15. 新 Design Token 系统

所有页面必须使用统一 token。

## 15.1 颜色

```css
--bg-0: #0d0c0a;
--bg-1: #15120e;
--surface-1: rgba(27, 23, 17, .92);
--surface-2: rgba(38, 31, 22, .92);
--paper-1: #d8c39a;
--paper-2: #bba579;
--text-1: #eee2c4;
--text-2: #bcae91;
--text-dark: #34291b;
--gold: #aa8449;
--gold-soft: #7c643b;
--vermillion: #8f3024;
--danger: #9c3b2e;
--success: #718a70;
--border: rgba(176, 139, 78, .25);
```

原则：

- 不再“一切都是金色”；
- 金色只表示当前焦点 / 权威 / 重要操作；
- 朱色只用于危险、证据、印记；
- 普通正文以灰米白为主。

---

## 15.2 圆角

```text
xs: 2px
sm: 4px
md: 6px
lg: 8px
```

不要现代 SaaS 16–24px 大圆角。

---

## 15.3 间距

采用 4px 基础网格：

```text
4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48
```

禁止出现随意 17px / 27px / 53px 等无体系间距。

---

# 16. Button 组件系统

参考 shadcn 的 variant + size 机制，但重做视觉。

统一组件：

```tsx
<GameButton
  variant="primary | secondary | ghost | danger | evidence"
  size="sm | md | lg | icon"
/>
```

## Primary

仅用于场景唯一主行动。

例如：

- 接下此案；
- 听取口供；
- 呈证质问；
- 提交推断；
- 觐见主公。

视觉：暗木 / 墨色底 + 细金边。

不允许发光。

---

## Secondary

用于：

- 查看文书；
- 再问一句；
- 打开证据。

---

## Ghost

用于：

- 返回；
- 案卷；
- 任务；
- 提示；
- 历史。

Ghost 只显示文字 / 图标和轻微背景，不要每个顶栏按钮都是厚方框。

---

## Danger

用于：

- 结束审讯；
- 强行定性；
- 重开章节。

---

# 17. Button 尺寸

```text
sm: 32px
md: 38px
lg: 44px
icon: 36×36px
```

普通按钮不超过 44px 高。

触摸目标可通过透明 padding 保证，不需要视觉上做成巨大木牌。

---

# 18. Card 组件系统

参考 shadcn Card 的结构：

```text
GameCard
├─ CardHeader
├─ CardContent
└─ CardFooter
```

同时增加：

```tsx
density="compact | default | spacious"
tone="dark | paper | transparent"
```

### 对白卡

使用 compact。

### 人物档案

使用 default。

### 军报原文

使用 paper。

禁止所有 Card 统一巨大 padding。

---

# 19. Dialogue 组件重构

统一：

```tsx
<DialoguePanel
  speaker={character}
  text={beat.text}
  portrait={beat.portrait}
  voice={beat.voice}
  mood={beat.mood}
/>
```

### 布局

对白区高度根据文本自动增长，但最大不超过屏幕 28%。

超过 4 行时内部滚动或分页。

建议中文单行宽度：

```text
28–42 汉字
```

不要整屏宽的大段文字。

---

# 20. Dialog / Sheet / Drawer 规则

参考 shadcn Dialog：

- 标题永远在 header；
- 正文可滚动；
- 操作固定在 footer；
- ESC 可关闭非关键弹层；
- 关闭按钮位置固定。

### 案卷

不再整页覆盖游戏，可采用大型 Sheet / Drawer：

```text
width: min(920px, 92vw)
height: min(780px, 90vh)
```

玩家关闭后回到原场景位置。

---

# 21. Tooltip 与锁定说明

参考 Aceternity Tooltip 思路，但关闭跟鼠标夸张移动。

例如锁定：

> 以笔迹异常质证

Hover：

> 完成“军书笔迹核验”后解锁。

Tooltip 延迟：250–400ms。

不是鼠标一碰就弹。

---

# 22. Animated List 的使用

借鉴 Magic UI Animated List，只用于：

- 新线索获得；
- 阶段总结；
- 案卷更新；
- 敌军回声；
- 最终人物结局。

例如：

```text
已查明
✓ 集合时辰曾被赵简接触
✓ 赵简口供存在时间矛盾
✓ 军书笔迹存在异常
```

逐条间隔 120–180ms 出现。

不能 1 秒一条，避免拖慢。

---

# 23. Blur Fade 的使用

推荐动画：

人物进入：

```text
duration 260ms
blur 4px → 0
translate 6px → 0
```

对白切换：

```text
180ms
opacity + 2px 位移
```

章节 CG：

```text
500–800ms
```

禁止所有内容统一 400ms 模糊淡入，容易产生“模板网站感”。

---

# 24. 避免“AI UI 感”的硬规则

以下内容列入 Code Review 禁止项。

## 24.1 禁止巨大标题覆盖人物

普通场景标题不得超过 30px 左右。

---

## 24.2 禁止随机斜线与装饰几何

除非与军旗、地图边界有语义关系。

当前“质询赵简”背景里的巨大斜切装饰应删除。

---

## 24.3 禁止一屏四种材质

一屏最多：

- 环境 CG；
- 一种深色 UI 材质；
- 一种纸张材质。

---

## 24.4 禁止每个区域都加边框

优先使用：

- 留白；
- 对齐；
- 明暗；
- 字号；
- 轻分隔线。

---

## 24.5 禁止所有按钮文字古语化

“案卷”“任务”“提示”可以现代易懂。

不要为了古风写成难懂词语。

---

## 24.6 禁止字体过大

尤其禁止把“场景标题”当海报主标题。

---

## 24.7 禁止所有控件金边

只有当前焦点 / 主操作使用更明显金边。

---

# 25. 字体策略

字体必须以可读为先。

推荐结构：

- 游戏标题：书法 / 古意展示字体；
- 章节标题：宋体 / 明朝体风格；
- 人物名：宋体偏粗；
- 对白正文：现代宋体 / 黑宋折中；
- UI 控件：清晰黑体 / 宋黑混合。

原则：

> 展示字 ≠ 正文字。

当前大标题的“书法展示感”不能下沉到所有页面。

---

# 26. 顶部导航重构

当前顶部“返回 / 案卷 / 任务 / 提示”都是厚按钮，会产生工具栏网页感。

新版：

```text
[←]  第一折 · 再问赵简                      案卷  任务  提示
```

高度：52px。

左侧返回只显示 icon + hover label。

右侧三个入口为 ghost button。

当前任务不再占一个大方框。

任务有变化时：

> 任务 · 1

用小红点提示。

---

# 27. 人物状态 UI 重构

当前右上“人物状态 + 彩色条”过于像 RPG 属性栏。

新版不显示彩条。

人物卡旁只显示：

> 状态：戒备

或者以小 Badge：

> 【戒备】

颜色非常克制。

玩家通过人物表情和台词感知变化，UI 只做辅助。

---

# 28. 新任务系统的节奏

任务不是一开始把所有调查点全列出来。

采用逐步解锁。

例如：

```text
任务：听取赵简口供
↓
任务：核对赵简是否接触时辰
↓
任务：建立第一条矛盾
↓
任务：再次问询赵简
↓
任务：调查其他信息渠道
```

玩家始终只面对 1 个主任务 + 最多 2 个可选行动。

---

# 29. 教程必须嵌入剧情

禁止：

> “欢迎来到游戏，这是推理板，这是审讯，这是任务……”

正确：

第一次用什么，才教什么。

### 第一次军报

教展开。

### 第一次发现冲突

教关系。

### 第一次再次问人

教审讯策略。

### 第一次卡住

提示“案卷 / 任务 / 提示”。

### 第一次自由调查

教人物—密报—任务关联。

---

# 30. CG 使用节奏

CG 不应当一直做背景。

## CG 只用于：

- 序章；
- 人物第一次亮相；
- 重大审讯突破；
- 曹操觐见；
- 投饵行动；
- 敌军回声；
- 结局。

普通调查页面以案桌和局部人物卡为主。

这样 CG 才有价值。

---

# 31. 音效节奏

进入新阶段：环境音先到。

人物出现：轻微衣料 / 脚步。

人物说话：重点句 AI 配音。

普通文字：不配音。

选择出现：不播夸张音。

玩家确认：轻木声。

线索成立：印章 / 笔触。

错误：不使用“错误 beep”，只轻微退回与纸张声。

---

# 32. 对话选择重构

选项控制在 2–4 条。

每条采用：

```text
[稳妥]
证据尚不足以定人。
```

不要每条都写很长。

Hover 时才显示解释：

> 更谨慎，通常不会提高对方戒备。

---

# 33. 响应式规则

## 1920×1080

完整布局。

## 1600×900

压缩左右辅助区。

## 1366×768

- 隐藏非核心描述；
- 人物图宽度下降；
- 对话区固定在底部；
- 顶栏仅图标 + 简短文字；
- Scene title 不超过 24px。

任何 1366×768 页面不得横向滚动，不得出现标题截断。

---

# 34. UI 组件目录建议

```text
src/ui/
  primitives/
    GameButton.tsx
    GameIconButton.tsx
    GameCard.tsx
    GameBadge.tsx
    GameTooltip.tsx
    GameDialog.tsx
    GameSheet.tsx
    Separator.tsx

  motion/
    BlurFade.tsx
    AnimatedList.tsx
    SceneTransition.tsx
    CharacterReveal.tsx

  game/
    SceneHeader.tsx
    DialoguePanel.tsx
    DialogueChoiceList.tsx
    CharacterIntro.tsx
    CharacterCard.tsx
    EvidenceCard.tsx
    EvidenceTray.tsx
    TaskChip.tsx
    TaskDrawer.tsx
    DossierSheet.tsx
    HintPanel.tsx
    DeductionLink.tsx
    ChapterTransition.tsx
```

页面禁止自己重新实现 button / card / modal。

---

# 35. 数据驱动叙事结构

建议升级为：

```ts
interface StoryBeat {
  id: string
  type:
    | 'narration'
    | 'character-intro'
    | 'dialogue'
    | 'choice'
    | 'tutorial'
    | 'evidence'
    | 'deduction'
    | 'transition'

  speakerId?: CharacterId
  text?: string
  voiceId?: string
  portraitVariant?: string
  camera?: 'wide' | 'medium' | 'close'
  choices?: StoryChoice[]
  unlocks?: string[]
}
```

页面只负责“如何演”。

故事数据负责“演什么”。

这样以后重排剧情节奏不需要重写 React 组件。

---

# 36. 开场 v0.9 推荐时长

```text
标题：用户自主
背景 CG：40–60 秒
身份介绍：15–20 秒
第一军报教程：30–60 秒
赵简第一次出场：30–45 秒
第一次文书调查：1–2 分钟
第一条推断：30–60 秒
第二次赵简对话：1–2 分钟
```

玩家大约 5–8 分钟后才进入真正“审讯”状态。

这比当前一开始就“质询赵简”更容易让玩家理解游戏。

---

# 37. 新玩家第一小时（第一案）体验曲线

```text
0–2 min   氛围与背景
2–5 min   学会看文书
5–8 min   认识赵简 / 第一矛盾
8–15 min  认识其他人物
15–22 min 理解人人有秘密
22–30 min 还原碎片泄密链
30–35 min 第一次曹操复命
35–45 min 主动投饵
45–50 min 敌军回声
50–60 min 最终汇报 / 政治结局
```

实际时长不设机械限制。

---

# 38. 首次试玩 UX 验收问题

测试玩家必须能在不看设计文档的情况下回答：

1. 我是谁？
2. 为什么我要查这个案子？
3. 赵简是谁？
4. 我现在下一步做什么？
5. 为什么我要看这份文书？
6. 我刚才发现了什么矛盾？
7. 如果我卡住，在哪里看任务？
8. 怎么返回？
9. 为什么赵简有嫌疑，但不能直接定罪？
10. 杜衡为什么危险？

若 3 个测试玩家中 2 个答不上，说明引导失败。

---

# 39. UI 视觉验收标准

以下任何一条出现，都不允许进入正式交付：

- 普通标题大于 40px 且占主要视觉；
- 1366×768 文本截断；
- 横向滚动；
- 同一屏超过 2 个主色强调；
- 同一屏超过 3 个高视觉权重 Card；
- 同一屏超过 2 个 Primary Button；
- 大量绝对定位导致缩放破坏；
- 人物脸被 UI 遮挡；
- CG 中烘焙了按钮和文字；
- 开发字段直接展示；
- 所有控件统一金色边框；
- 页面第一眼看起来像 AI 生成宣传图。

---

# 40. 动效验收标准

所有动效必须回答：

> 它在告诉玩家什么？

如果回答只是“更酷”，删除。

同时实现：

```css
@media (prefers-reduced-motion: reduce)
```

减少动画模式。

---

# 41. 对当前“质询赵简”页面的具体改造

## 当前问题

- 过大标题；
- 页面上半部浪费；
- 顶栏厚；
- 赵简被视觉遮挡；
- 口供卡大而空；
- 状态条像数据面板；
- 用户不知道为何此刻要质询。

## 改成

```text
┌─────────────────────────────────────────┐
│ ←  第一折 · 再问赵简         案卷 任务 提示 │
├─────────────────────────────────────────┤
│                                         │
│      [赵简中景 / 审讯 CG]                │
│                                         │
│  赵简 · 军书佐          [戒备]           │
│  “属下并不知道最终集合时辰。”            │
│                                         │
│  已知矛盾：口供 ↔ 集合记录                │
│                                         │
│   平询    缓劝    [呈证质问]    设问      │
│                                         │
└─────────────────────────────────────────┘
```

不再显示巨大“质询赵简”。

---

# 42. 开发实施顺序

## P0：先解决“看起来不像游戏”

1. Font scale 全局收敛；
2. SceneHeader 重写；
3. Button variants / sizes；
4. Card density；
5. 1366×768 响应式；
6. 去除巨大装饰几何；
7. 赵简页面重新布局。

---

## P1：重排前 10 分钟游戏流程

1. 背景序章；
2. 身份介绍；
3. 嵌入式新手教程；
4. 赵简首次人物介绍；
5. 第一口供；
6. 第一文书；
7. 第一推断；
8. 第二次赵简对话。

---

## P2：统一人物节奏

陆淳 / 郑禾 / 杜衡全部新增首次出场流程。

---

## P3：高级 UI

1. Dossier Sheet；
2. Task Drawer；
3. Tooltip；
4. Animated List；
5. Blur Fade；
6. 对话历史。

---

## P4：CG 与声音

在流程稳定后再完善：

- CG 分层；
- 人物表情变体；
- AI 正式声线；
- 环境声音混音。

---

# 43. 下一版完成定义

v0.9 不以“加了多少组件”判断完成。

必须满足：

### 第一印象

普通用户不会首先评价“这是 AI UI”。

### 流程

新玩家先理解背景和身份，再进入案件。

### 人物

第一次见每个关键人物都有明确人物介绍。

### 对话

人物出场 → 说话 → 玩家回应 → 产生任务 / 证据。

### 推理

第一条推断由剧情自然教学，而不是打开一个复杂推理板。

### 控件

所有 Button / Card / Dialog / Sheet 来自统一组件系统。

### 字体

1366×768 不溢出、不截断。

### 导航

任何时候知道：我在哪、我在做什么、怎么返回。

---

# 44. 本轮最重要的一句话

下一版《官渡密报》不应该再按照：

> **“有什么功能，就把什么功能放到屏幕上。”**

而应该按照：

> **“玩家此刻需要理解什么，就只给他看这一层信息。”**

这将是从“AI 原型感”走向真正游戏体验的关键。


---

# 45. v0.9.0 实施同步状态

截至 v0.9.0 基线重置，本文件的 P0–P3 主要结构已经进入代码：

- Design Token、Button、Card、Sheet、Tooltip、SceneHeader 等组件体系已建立；
- 标题页、1366 响应式与普通标题尺度已收敛；
- 开场已重排为背景 → 身份 → 基础引导 → 赵简首次出现 → 第一口供；
- 第一证据 / 第一推断已改成引导式玩法；
- 再问赵简已使用紧凑人物 + 对白 + 策略布局；
- 陆淳 / 郑禾 / 杜衡先人物介绍再调查；
- 中段泄密链改为逐问推进；
- 曹操复命改为单 CG 主视觉；
- 投饵改为逐渠道操作；
- 敌军回声改为单急报；
- 最终报告改为事实 / 证据链 / 处置三步；
- 案卷人物页改为单人物浏览；
- 对话历史已进入案卷与存档；
- 结局页改为单主卡结构。

P4 高级表现仍属于下一阶段：

- 完整人物情绪图；
- 正式 AI 配音；
- CG 2.5D 分层；
- 目标机多分辨率人工视觉验收。

完成定义仍以第 43 节为准，不因为“组件文件已经存在”就视为视觉完成。


# 46. v0.9 存档与恢复实施补充

- 当前存档 schema：v5；
- v0.8.x 的案件证据、人物状态、调查结果继续保留；
- v0.8.x 的旧演出位置和导航历史不继续沿用；
- 旧 dialogue 存档映射到赵简首次对白；
- 当前 v5 存档若混入 legacy history，会在加载/恢复时过滤；
- SceneRecovery 只提供 v0.9 合法恢复节点。

这条规则保证“后续项目重置”不仅体现在 UI，也体现在老玩家真实加载存档后的体验。
