# 《官渡密报》v0.9.2 审核验收报告

## 本轮修订范围

- 标题页启动体验与流程导引；
- 场景按钮对比度；
- 证据卡按钮可见性；
- 标题背景自适应；
- 主要人物展示区比例稳定；
- 顶栏“当前任务”快捷入口。

## 已完成检查

### 1. 启动与存档
- 启动仍从标题页进入；
- 进入后再查看存档；
- 存档页保留当前进度摘要。

### 2. 视觉交互
- 主按钮 / 次按钮 / 幽灵按钮对比度提高；
- 证据卡浅底场景下按钮进一步加深；
- 禁用态按钮仍保留边框与底色。

### 3. 标题页与自适应
- 标题页高度锁定视口，避免额外拉长；
- 超宽屏下背景居中裁切，减少右侧异常延展观感。

### 4. 人物呈现
- 主要人物场景统一安全框；
- 减少跨场景高度突变与压缩变形。

### 5. 导向性
- 标题页新增游玩流程说明；
- 顶栏新增“当前任务”；
- 案卷任务页仍承担详细目标说明。

## 验证

- `npm run verify:v091`：通过。
- `npm run verify:v09:deep`：未在当前容器执行（依赖安装未完成时可能受网络影响），建议在开发机继续执行 `npm ci && npm run build && npm run test:run`。

## 结论

v0.9.2 已完成一次以“看得清、点得准、知道下一步”为目标的体验收束，可作为下一轮试玩包。
