# 《官渡密报》v0.9.1 试玩修订与存档系统审核报告

> 审核日期：2026-08-28  
> 范围：v0.9 叙事/UI基线之上的试玩可用性修订。

## 1. 本轮必须解决的问题

1. 启动后不能自动跳回中途，必须先由玩家选择存档；
2. 必须能在进入游戏前知道自己玩到哪里；
3. 第一证据比较必须稳定可点击，查看详情不能破坏左右布局；
4. 人物图片不得被任意容器拉伸；
5. 暗色背景下按钮、选中态和 disabled 状态必须清楚。

## 2. 已实装

- 三个固定本地存档槽；
- 存档摘要：幕 / 场景 / 当前任务 / 进度 / 线索 / 推断 / 最后保存时间；
- 服务器/页面启动始终进入标题；
- 玩家点击存档后才恢复状态；
- 当前槽位自动保存；
- 旧 `guandu.current` 第一次自动导入槽 1，且只迁移一次；
- 第一证据页不再用 `button > article` 的无效嵌套结构；
- “选择这条 / 取消选择 / 查看详情 / 重置选择 / 比较”独立；
- 详情使用 Dialog，关闭后原卡片尺寸不变；
- 证据页明确显示 `已选择 N / 2` 和缺失数量；
- 人物资源统一 `object-fit: contain`；
- 赵简审讯背景改为独立军帐背景，不再把带赵简的 CG 作为整页背景；
- 顶栏显示当前活动存档与自动保存状态；
- 主按钮 / 次按钮 / disabled 状态提高对比度。

## 3. 自动审核

执行：

```bash
npm run verify:v091
npm run verify:v091:deep
```

静态合同覆盖原 v0.9 全部合同，并新增 save/evidence/portrait 合同。深度合同继续执行旧存档迁移 runtime-lite 与全 TS/TSX 语法转译。

## 4. 仍需人工试玩确认

- 1366×768 下三张存档卡是否需要进一步压缩；
- Windows Chrome / Edge 中证据按钮点击区域是否稳定；
- 赵简及其他人物的源人物图虽然已禁止拉伸，但是否仍需要进一步重新生成人脸比例；
- 五名人物的真正透明 PNG/WebP 抠像仍建议作为下一轮美术资源工作完成。

## 5. 完成定义

v0.9.1 以后：

> 启动不等于读档；玩家选择存档才等于读档。  
> 选择改变状态，查看只打开详情。  
> 人物不承担背景职责，背景不决定人物比例。  
> 克制不等于模糊，可操作元素必须清楚。
